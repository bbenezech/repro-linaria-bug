import React from "react";

// break me! (change <span> to <spa> for ex.)
// then try to revert => webpack won't peak up changes
const Other = ({ children }) => <span>{children}</span>;
export default Other;
